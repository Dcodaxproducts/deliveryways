# Tasks

1. Extend and validate the backend landing settings DTO/shape.
   - Verify DTO tests and TypeScript.
2. Add rich-text sanitization and dedicated landing settings endpoints.
   - Verify service/controller tests and NestJS per-file checks.
3. Add Superadmin types, service hooks, route, sidebar entry, localized editor, and FAQ manager.
   - Verify EN/DE key parity, lint, TypeScript, and build.
4. Extend the landing provider and add managed page components/routes.
   - Verify locale selection, sanitization tests, lint, TypeScript, and build.
5. Remove FAQ fallback rendering and static contact details.
   - Verify empty FAQ/contact states.
6. Run full project verification, review diffs, commit, and push each repository.
