name: deliveryway-staging

services:
  api:
    environment:
      APP_ENV: staging
    ports:
      - 127.0.0.1:6050:3000

  restaurant-admin:
    ports:
      - 127.0.0.1:6051:3000

  superadmin:
    ports:
      - 127.0.0.1:6052:3000

  customer:
    ports:
      - 127.0.0.1:6053:3000

  landing:
    ports:
      - 127.0.0.1:6054:3000
